{
    "name": "angular2-color-picker",
    "version": "2.0.0",
    "scripts": {
        "start": "rm -rf ../lib && npm run tsc",
        "postinstall": "typings install",
        "tsc": "tsc -p src/color-picker",
        "typings": "typings"
    },
    "license": "MIT",
    "dependencies": {
        "@angular/core": "2.0.0-rc.1",
        "core-js": "^2.4.0",
        "reflect-metadata": "^0.1.3",
        "rxjs": "5.0.0-beta.6",
        "zone.js": "^0.6.12"
    },
    "devDependencies": {
        "concurrently": "^2.0.0",
        "typescript": "^1.8.10",
        "typings": "^1.0.4"
    }
}
